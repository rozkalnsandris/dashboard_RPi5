# Master roadmap pointer

The canonical live product contract is GitHub issue **#1 — `[MASTER / READ FIRST] dashboard_RPi5 — product contract, architecture and delivery roadmap`**.

Repository mirror/index: [`ROADMAP.md`](ROADMAP.md).

Rules:

- issue #1 is canonical for current phase/status and owner gates;
- `ROADMAP.md` mirrors the durable delivery plan inside the repository;
- implementation must not silently diverge from either;
- if the contract changes, update issue #1 and the repository mirror in the same governed work item.
