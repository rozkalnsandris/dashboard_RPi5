{
  "name": "@dashboard-rpi5/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsup src/index.ts --format esm --platform node --target node24 --clean"
  }
}
