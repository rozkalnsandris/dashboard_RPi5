{
  "name": "@dashboard-rpi5/terminal-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:native": "tsx tools/native-smoke.ts",
    "test:native:packaged": "tsx tools/packaged-native-smoke.ts",
    "build": "tsup src/index.ts src/session-stdio-entry.ts --format esm --platform node --target node24 --clean"
  },
  "dependencies": {
    "node-pty": "1.1.0"
  }
}
