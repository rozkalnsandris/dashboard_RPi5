# SOURCE-ONLY EXAMPLE. Do not copy to production without a fresh LIVE preflight.
# The placeholder is intentionally invalid; the exporter must fail closed until
# an exact private IPv4 reachable from the Prometheus container is reviewed.
DASHBOARD_CONTAINER_METRICS_LISTEN_HOST=__FRESH_PRIVATE_IPV4__
DASHBOARD_CONTAINER_METRICS_LISTEN_PORT=9464
